{
  "name": "lectureai-open-source-core",
  "version": "0.1.0",
  "description": "Local-first lecture recording and study workspace for the web.",
  "type": "module",
  "scripts": {
    "check": "node --check src/app.js && node --check src/recorder.js && node --check src/storage.js && node --check src/diagnostics.js && node --check src/utils.js && node --check sw.js",
    "test": "node --test",
    "ci": "npm run check && npm test"
  },
  "keywords": [
    "education",
    "lecture",
    "recording",
    "local-first",
    "pwa"
  ],
  "license": "MIT"
}
