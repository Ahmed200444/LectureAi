{
  "name": "lectureai-deploy",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "rm -rf app && mkdir -p app && tar -xzf lectureai-vercel-source.tar.gz -C app && cd app && npm install --no-audit --no-fund && npm run build"
  }
}
